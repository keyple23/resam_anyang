# 네이버 웹사이트탭 공략용 아티클 사이트 — 운영 가이드

목표는 하나입니다. **네이버 검색창에 특정 키워드를 쳤을 때 웹사이트탭에 우리 사이트가 뜨게 하는 것.**
덤으로 챗GPT·제미나이 같은 AI 답변에 인용되도록 구조화 데이터와 크롤러 허용까지 넣어뒀습니다.

본 쇼핑몰 도메인은 건드리지 않습니다. `news.브랜드도메인.com` 같은 서브도메인에 아티클을 쌓는 방식입니다.

---

## 목차

1. [먼저 알아둘 것](#1-먼저-알아둘-것)
2. [폴더 구조](#2-폴더-구조)
3. [GitHub에 올리기](#3-github에-올리기)
4. [Vercel로 배포하기](#4-vercel로-배포하기)
5. [도메인 연결하기](#5-도메인-연결하기)
6. [네이버 서치어드바이저 등록](#6-네이버-서치어드바이저-등록-제일-중요합니다)
7. [구글 서치콘솔 등록](#7-구글-서치콘솔-등록-선택)
8. [관리자 페이지 열기](#8-관리자-페이지-열기)
9. [키워드 추가하기](#9-키워드-추가하기)
10. [글 쓰기](#10-글-쓰기)
11. [이미지 넣기](#11-이미지-넣기)
12. [새 글 올린 뒤 할 일](#12-새-글-올린-뒤-할-일)
13. [순위가 안 오를 때 점검 순서](#13-순위가-안-오를-때-점검-순서)

---

## 1. 먼저 알아둘 것

### 걸리는 시간

| 단계 | 소요 |
|---|---|
| GitHub 업로드 + Vercel 배포 | 20분 |
| 도메인 연결 (DNS 반영 대기 포함) | 30분 ~ 몇 시간 |
| 서치어드바이저 등록 | 20분 |
| 네이버 색인 시작 | 며칠 ~ 2주 |
| 웹사이트탭 순위 형성 | 1개월 이상 |

**등록했다고 다음 날 뜨지 않습니다.** 글이 쌓이는 속도와 색인 속도가 순위를 만듭니다.

### 글이 몇 개나 있어야 하나

글 5~6개로는 순위가 안 올라갑니다. 최소 15~20편을 목표로 잡으세요.
큰 키워드 하나에 글 하나를 붙이는 게 아니라, 키워드를 쪼개서 각각 하나씩 씁니다.

### 비용

Vercel 무료 플랜이면 충분합니다. 정적 사이트라 서버 비용이 들지 않습니다.
도메인 값만 나갑니다. 서브도메인을 쓰면 그것도 안 듭니다.

---

## 2. 폴더 구조

```
프로젝트폴더/
├─ app/              화면과 검색엔진용 파일. 손댈 일 거의 없음
├─ components/       카드, 커버 같은 부품. 손댈 일 거의 없음
├─ lib/              동작 코드. 손댈 일 없음
├─ content/
│   ├─ settings.json ★ 브랜드 정보 (관리자 화면에서 수정 가능)
│   ├─ keywords/     ★ 노출을 노리는 키워드. 하나당 파일 하나
│   └─ articles/     ★ 아티클 원고
├─ public/
│   ├─ images/       ★ 사진과 커버 이미지
│   └─ logo.png      ★ 브랜드 로고
├─ tools/            임시 커버 이미지 생성기
│                    (/admin 화면에서 다 할 수 있으니 안 써도 됩니다)
└─ 가이드.md          이 파일
```

★ 표시된 네 군데만 손대면 됩니다. 나머지 코드는 그대로 씁니다.

다만 배포가 끝난 뒤에는 이 폴더를 직접 만질 일이 거의 없습니다.
**`/admin` 관리자 화면에서 브라우저로 전부 할 수 있습니다** (8번 참고).

자동으로 만들어지는 것들 (신경 안 써도 됨):

| 항목 | 설명 |
|---|---|
| sitemap.xml | 글을 추가하면 자동 반영. 이미지 주소까지 포함 |
| rss.xml | 서치어드바이저 RSS 제출용 |
| robots.txt | 네이버 Yeti, 구글, 챗GPT, 클로드, 퍼플렉시티 등 명시 허용 |
| llms.txt | AI가 사이트 전체를 한 번에 파악하도록 정리한 파일 |
| JSON-LD | Article, BreadcrumbList, FAQPage, Organization, ImageObject |
| canonical | 글마다 정식 주소 지정 |
| 오픈그래프 | 카카오톡 공유 미리보기 |
| 목차 | 소제목 3개 이상이면 자동 생성 |
| 관련글 | 같은 카테고리 우선으로 자동 연결 |

---

## 3. GitHub에 올리기

1. `github.com` 로그인 → 오른쪽 위 `+` → `New repository`
2. 이름을 정하고 **Private** 선택 → `Create repository`
3. `Add file` → `Upload files`
4. **압축을 푼 폴더를 통째로 끌지 말고, 폴더를 열어서 안에 있는 것 전부를 선택해 끌어다 놓습니다**

   이게 제일 많이 틀리는 부분입니다. 폴더째 끌면 `프로젝트폴더/app/...` 이렇게 한 겹 더 들어가서 빌드가 깨집니다.

5. 아래 `Commit changes` 클릭

`node_modules`, `.next`, `.env` 는 올리지 않습니다. `.gitignore` 에 이미 걸려 있습니다.

> 크롬 자동 번역이 켜져 있으면 폴더 이름이 `앱`, `구성 요소`, `공공의` 처럼 보입니다.
> 실제 이름은 `app`, `components`, `public` 입니다. 번역 끄고 보는 게 헷갈리지 않습니다.

---

## 4. Vercel로 배포하기

1. `vercel.com` → `Continue with GitHub` 로 가입
2. `Add New` → `Project`
3. 방금 만든 저장소 옆 `Import`
4. Framework 는 Next.js 로 자동 인식됩니다. 아무것도 건드리지 말고 `Deploy`
5. 1~2분 뒤 축하 화면이 나옵니다

이 시점에 `프로젝트이름.vercel.app` 주소로 사이트가 이미 열립니다.
도메인을 붙이기 전에 화면부터 확인해보세요.

### 환경변수 넣기

`Settings` → `Environment Variables` 에서 추가합니다.

| Key | Value |
|---|---|
| `NEXT_PUBLIC_SITE_URL` | `https://news.브랜드도메인.com` (한글 도메인은 퓨니코드로) |
| `NEXT_PUBLIC_NAVER_VERIFY` | 6단계에서 받는 코드 |
| `NEXT_PUBLIC_GOOGLE_VERIFY` | 7단계에서 받는 코드 (선택) |

**Type 은 반드시 `Config` 로 두세요.** `Secret` 으로 넣으면 경고가 뜨고 화면에 값이 안 박힙니다.
`NEXT_PUBLIC_` 으로 시작하는 값은 브라우저에 노출되는 게 정상입니다. 소유확인 코드는 원래 공개되는 값입니다.

**환경변수를 넣거나 바꾼 뒤에는 반드시 재배포해야 반영됩니다.**
`Deployments` → 맨 위 항목 오른쪽 `...` → `Redeploy` → `Redeploy`

---

## 5. 도메인 연결하기

본 도메인은 그대로 두고 서브도메인만 붙입니다. 기존 쇼핑몰은 영향받지 않습니다.

1. Vercel 프로젝트 → `Settings` → `Domains`
2. `news.브랜드도메인.com` 입력 → `Add`
3. Vercel이 `CNAME` 값을 알려줍니다 (`xxxxx.vercel-dns-017.com` 형태)
4. 도메인을 산 곳의 DNS 관리 화면에서 레코드를 추가합니다

| 타입 | 호스트 | 값 |
|---|---|---|
| CNAME | `news` | Vercel이 알려준 값 |

5. 저장하고 기다립니다. 보통 몇 분, 길면 몇 시간
6. Vercel 화면이 `Invalid Configuration` → `Valid Configuration` 으로 바뀌면 됩니다
7. SSL 인증서는 그 뒤 자동으로 발급됩니다 (`Generating SSL Certificate` 표시)

### 한글 도메인일 때

DNS 입력칸에 한글이 안 들어가는 곳이 많습니다. 퓨니코드로 변환해서 넣으세요.
`웰컴드링크.com` → `xn--hy1bw6gi3nsyh1zb.com` 형태입니다. 변환은 "퓨니코드 변환기"로 검색하면 나옵니다.

`content/settings.json` 의 `url` 과 Vercel 환경변수에도 퓨니코드로 적는 게 안전합니다.

### 등록업체별 DNS 화면 찾는 법

- **아임웹**: 도메인 → 해당 도메인 → `네임서버 설정` 버튼 안에 DNS 레코드 편집기가 있습니다. `...` 메뉴에는 없습니다
- **가비아**: My가비아 → DNS 관리툴 → 도메인 선택 → `DNS 설정`
- **카페24**: 도메인 관리 → DNS 관리
- **후이즈**: 도메인 관리 → DNS 정보 변경

---

## 6. 네이버 서치어드바이저 등록 (제일 중요합니다)

**이 단계를 빼먹으면 글을 아무리 잘 써도 웹사이트탭에 절대 안 나옵니다.**

1. `searchadvisor.naver.com` 접속 → 네이버 계정으로 로그인
2. `웹마스터 도구` → 사이트 등록에 `https://news.브랜드도메인.com` 입력
3. 소유확인 방법에서 **HTML 태그** 선택
4. 화면에 이런 게 나옵니다. `content="..."` 안의 **값만** 복사합니다

   ```html
   <meta name="naver-site-verification" content="여기값만복사" />
   ```

5. Vercel → `Settings` → `Environment Variables` → `NEXT_PUBLIC_NAVER_VERIFY` 에 붙여넣기 (Type: Config)
6. **Vercel에서 Redeploy** (4단계 마지막 참고). 이걸 안 하면 소유확인이 실패합니다
7. 서치어드바이저로 돌아와 `소유확인` 클릭
8. 확인되면 왼쪽 메뉴에서 아래를 차례로 합니다

| 메뉴 | 넣을 값 |
|---|---|
| 요청 → 사이트맵 제출 | `sitemap.xml` |
| 요청 → RSS 제출 | `rss.xml` |
| 요청 → 웹페이지 수집 | 홈 주소 + 키워드 페이지 주소들 |
| 검증 → 웹페이지 최적화 | 점검만 (수정할 게 있으면 알려줍니다) |

사이트맵과 RSS는 입력칸에 도메인이 이미 적혀 있고 뒷부분만 치는 방식입니다.

---

## 7. 구글 서치콘솔 등록 (선택)

네이버가 우선이지만 같이 해두면 좋습니다.

1. `search.google.com/search-console` 접속
2. `URL 접두어` 방식 선택 → 주소 입력
3. 소유확인에서 `HTML 태그` 선택 → content 값 복사
4. Vercel 환경변수 `NEXT_PUBLIC_GOOGLE_VERIFY` 에 넣고 Redeploy
5. 확인되면 `Sitemaps` 에 `sitemap.xml` 제출

---

## 8. 관리자 페이지 열기

여기까지 하면 배포는 끝입니다. 이제부터 글쓰기, 이미지 올리기, 브랜드 설정은
파일을 만지지 않고 브라우저에서 바로 할 수 있습니다.

주소 뒤에 `/admin` 을 붙이면 관리자 화면이 나옵니다.

```
https://news.브랜드도메인.com/admin
```

이 화면은 검색엔진에 노출되지 않습니다. 주소를 아는 사람만 들어올 수 있고,
아래에서 만드는 토큰이 있어야 실제로 저장할 수 있습니다.

### 접속용 토큰 만들기

한 번만 만들면 됩니다.

1. `github.com` 오른쪽 위 프로필 사진 → **Settings**
2. 왼쪽 메뉴 맨 아래 **Developer settings**
3. **Personal access tokens** → **Fine-grained tokens** → `Generate new token`
4. Token name 은 아무 이름이나 (예: 매거진 관리자)
5. Expiration 은 원하는 기간으로 (1년 권장)
6. **Repository access** → `Only select repositories` → 이 사이트 저장소 선택
7. **Permissions** → Repository permissions → **Contents** 를 **Read and write** 로 변경
8. 맨 아래 `Generate token` → 나온 값을 복사

**토큰은 이때 한 번만 보여줍니다.** 창을 닫기 전에 복사하세요. 잃어버리면 새로 만들면 됩니다.

### 로그인

관리자 화면에 세 가지를 넣습니다.

| 칸 | 넣을 값 |
|---|---|
| GitHub 사용자명 | 저장소 주소의 `github.com/`**여기**`/저장소이름` |
| 저장소 이름 | 저장소 주소의 마지막 부분 |
| 액세스 토큰 | 방금 복사한 값 |

한 번 넣으면 그 브라우저에 기억됩니다. 다음부터는 주소만 치면 바로 들어갑니다.
입력한 값은 브라우저에만 저장되고 어디로도 전송되지 않습니다.

### 관리자 화면에서 할 수 있는 것

| 탭 | 하는 일 |
|---|---|
| 키워드 | 노출을 노리는 키워드 추가·수정·삭제 |
| 글 | 아티클 작성·수정·삭제 |
| 이미지 | 사진을 끌어다 놓아 업로드, 삭제 |
| 브랜드 설정 | 사이트 이름, 설명, 주소, 분류 변경 |
| 도움말 | 글쓰기 규칙과 본문 표기법 |

**저장을 누르면 GitHub에 바로 반영되고, Vercel이 1~2분 안에 사이트를 다시 만듭니다.**
바로 안 보이면 잠시 뒤 새로고침하세요.

아래 9~11번은 관리자 화면의 각 칸이 무슨 뜻인지 설명한 것입니다.
파일을 직접 고치고 싶을 때의 형식도 같이 적어뒀습니다.

---

## 9. 키워드 추가하기

가장 자주 하게 될 작업입니다.

노출을 노리는 키워드 하나가 파일 하나입니다.
`content/keywords/` 에 `.md` 파일을 만들면 그 키워드 전용 페이지가 자동으로 생깁니다.

파일 하나를 추가하면 아래가 전부 자동으로 따라옵니다.

- `/keywords/키워드이름/` 페이지 생성
- 홈 "주제별로 보기"에 카드 추가
- 헤더와 푸터 메뉴에 링크 추가
- 사이트맵·llms.txt에 등록
- 홈 타이틀에 키워드 반영 (priority 높은 순 3개)

### 파일 형식

`content/keywords/_키워드템플릿.md` 를 복사해서 씁니다.

```markdown
---
slug: "무니코틴액상"
keyword: "무니코틴 액상"
variants: ["무니코틴액상", "무니코틴 전자담배"]
title: "무니코틴 액상이란 | 니코틴 액상과 차이, 고를 때 확인할 것"
description: "검색 결과에 그대로 나오는 두 문장"
priority: 90
cover: "kw-no-nicotine.jpg"
coverAlt: "이미지에 무엇이 찍혀 있는지 한 문장"
articles:
  - 연결할-아티클-파일명
faq:
  - q: "질문"
    a: "답"
---

본문
```

| 항목 | 설명 |
|---|---|
| `slug` | 주소에 들어갈 이름. 한글도 됩니다 |
| `keyword` | 화면에 표시되고 검색을 노리는 말 |
| `variants` | 띄어쓰기 유무 등 같은 뜻의 다른 표기 |
| `title` | 검색 결과 제목. **키워드를 맨 앞에** 두세요 |
| `priority` | 숫자가 클수록 홈 타이틀과 메뉴에서 앞에 옵니다 |
| `articles` | 이 키워드와 묶을 아티클 파일명 |
| `draft: true` | 사이트에 안 나옵니다 |

### 홈 타이틀이 만들어지는 방식

웹문서탭 상위 사이트들이 쓰는 패턴을 그대로 따릅니다.
priority 높은 순으로 키워드 3개를 나열하고 브랜드명을 붙입니다.

```
전자담배 액상 | 무니코틴 액상 | 전자담배 - 웰컴드링크 매거진
```

키워드 파일을 바꾸면 이 제목도 자동으로 바뀝니다.

### 어떤 키워드를 고를 것인가

웹사이트탭에 이미 잡혀 있는 곳들의 공통점입니다.

- 제목이 질문이거나 비교입니다
- 지역명이나 카테고리명이 제목에 들어갑니다
- 한 글이 한 키워드만 노립니다

검색량이 가장 많은 대표 키워드는 보통 쇼핑몰 메인이 차지하고 있어서 아티클로는 잘 안 뚫립니다.
검색량 2~4위권의 정보성 키워드부터 잡는 게 빠릅니다.
대표 키워드는 글이 20개 이상 쌓이고 사이트가 인정받은 뒤에 노립니다.

---

## 10. 글 쓰기

`content/articles/` 폴더에 `.md` 파일 하나가 글 하나입니다.
파일명이 주소가 됩니다. `nicotine-strength.md` → `/articles/nicotine-strength/`

**파일명은 영문 소문자와 하이픈만 씁니다.** 한글 파일명은 주소가 깨집니다.

### 파일 맨 위에 넣는 정보

```markdown
---
title: "여기에 글 제목"
description: "검색 결과에 그대로 나오는 두 문장. 결론을 먼저."
category: guide
published: "2026-01-01"
updated: "2026-01-01"
keywords: ["타겟 키워드", "관련 키워드"]
cover: "대표이미지.jpg"
coverAlt: "이미지에 무엇이 찍혀 있는지 한 문장"
author:
  name: "브랜드 편집팀"
faq:
  - q: "자주 묻는 질문"
    a: "두세 문장으로 된 답"
---
```

`category` 에 넣을 수 있는 값은 `lib/site.js` 의 categories 에 정의돼 있습니다.

### 본문 규칙

이 규칙을 지켜야 웹사이트탭과 AI 인용 양쪽에 걸립니다.

1. 첫 문단에서 결론부터 말합니다. 배경 설명은 뒤로 뺍니다
2. 소제목(`## `)은 고객이 실제로 던지는 질문 형태로 씁니다
   - 나쁜 예: `코일 관리`
   - 좋은 예: `코일은 언제 갈아야 하나요`
3. 소제목 바로 아래 두세 문장으로 답부터 씁니다
4. 비교할 게 있으면 표로 만듭니다. AI가 표를 그대로 가져갑니다
5. 확인되는 숫자만 적습니다. 없는 숫자는 만들지 않습니다
6. 타겟 키워드를 제목, 첫 문단, 소제목 하나에 자연스럽게 넣습니다
7. 다른 글로 가는 링크를 본문에 하나 이상 넣습니다

### 분량

소제목 4~6개, 전체 1500~3000자. 짧으면 색인은 되어도 순위가 안 오르고, 너무 길면 끝까지 안 읽힙니다.

---

## 11. 이미지 넣기

이미지 파일은 전부 `public/images/` 폴더에 넣습니다.
넣고 나면 **파일명만** 적으면 됩니다. 경로를 따로 쓸 필요가 없습니다.

### 글 맨 위 대표 이미지

```yaml
cover: "액상-비교.jpg"
coverAlt: "액상 세 종류를 나란히 놓고 니코틴 함량 표기를 비교한 사진"
coverCaption: "사진 밑에 붙는 설명. 필요 없으면 이 줄을 지우세요"
```

`coverAlt` 가 제일 중요합니다. 검색엔진과 AI는 사진을 못 봅니다. 이 문장을 읽고 사진 내용을 판단합니다.
`"이미지1"` 처럼 적으면 없는 것과 같습니다. 무엇이 찍혀 있는지 한 문장으로 적으세요.

대표 이미지는 카드 썸네일, 카카오톡 공유 미리보기, 이미지 사이트맵에 자동으로 함께 쓰입니다.

### 본문 중간 이미지

```markdown
![무엇이 찍혀 있는지 한 문장](파일이름.jpg "사진 밑에 붙을 설명")
```

두 장을 나란히 두려면:

```markdown
<div class="fig-row">

![왼쪽 사진 설명](왼쪽.jpg)

![오른쪽 사진 설명](오른쪽.jpg)

</div>
```

### 이미지가 없는 글

색깔 배경에 카테고리 이름이 들어간 카드가 자동으로 깔립니다. 화면이 비어 보이지는 않습니다.
다만 실제 사진이 있는 글이 클릭률이 높습니다. 가능하면 글마다 한 장은 넣으세요.

### 임시 커버 이미지 만들기

실제 사진이 준비되기 전이라면 브랜드 색상에 맞춘 임시 커버를 자동으로 만들 수 있습니다.

```bash
pip install pillow
python3 tools/커버만들기.py
```

만들 목록과 색상은 `tools/커버목록.json` 에서 바꿉니다.

### 사진 규격

| 항목 | 권장 |
|---|---|
| 대표 이미지 | 가로 1200px 이상, 16:9 |
| 공유 미리보기 | 1200 × 630 |
| 용량 | 한 장당 300KB 이하 |
| 파일명 | 영문 소문자와 하이픈 |

실제 사진으로 교체할 때는 **같은 파일명으로 덮어쓰면** 코드를 안 건드려도 됩니다.

---

## 12. 새 글 올린 뒤 할 일

1. GitHub에 파일 업로드 (또는 수정)
2. Vercel이 자동으로 재배포합니다 (1~2분)
3. 사이트에서 글이 보이는지 확인
4. 서치어드바이저 → `요청` → `웹페이지 수집` 에 그 글 주소를 넣습니다

4번을 하면 색인이 눈에 띄게 빨라집니다. 글 하나 올릴 때마다 습관처럼 하세요.

---

## 13. 순위가 안 오를 때 점검 순서

위에서부터 순서대로 확인합니다.

1. **소유확인이 되어 있나** — 서치어드바이저에서 사이트가 목록에 있는지
2. **사이트맵·RSS가 제출됐나** — 제출 상태에 오류가 없는지
3. **색인이 되고 있나** — 서치어드바이저 `검색 노출 / 수집 현황` 에서 수집 건수 확인
4. **네이버에서 `site:도메인` 으로 검색해보기** — 결과가 0이면 아직 색인 전입니다
5. **글 수가 충분한가** — 5~6개면 부족합니다. 15개 이상
6. **제목에 키워드가 맨 앞에 있나** — 뒤에 있으면 약합니다
7. **글끼리 링크가 걸려 있나** — 고립된 글은 평가를 못 받습니다
8. **본문이 검색 의도에 맞나** — 그 키워드로 검색한 사람이 원한 답이 첫 문단에 있는지

여기까지 다 맞는데도 두 달 넘게 변화가 없으면, 그 키워드는 아티클로 뚫기 어려운 구조일 수 있습니다.
경쟁 페이지가 전부 쇼핑몰 메인이면 키워드를 더 잘게 쪼개는 쪽으로 방향을 바꾸세요.

---

## 참고: 로컬에서 미리보기

Node.js 가 설치돼 있어야 합니다.

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:3000` 으로 열립니다.

---

## 참고: 규제 확인

업종에 따라 온라인 광고·판매에 규제가 있습니다.
이 사이트는 판매 페이지가 아니라 사용 정보를 다루는 구조로 만들어져 있지만,
어디까지가 광고로 해석되는지는 업종별로 다릅니다.
규제 업종이라면 운영 전에 법률 검토를 받고, 필요하면 본문 문구와 브랜드 사이트 링크 노출 방식을 조정하세요.
